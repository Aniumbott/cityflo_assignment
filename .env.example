# =====================================
# Cityflo Invoice Processing System
# Environment Variables Template
# =====================================
# Copy this file to .env and fill in your actual values
# IMPORTANT: Never commit .env to version control

# =====================================
# DATABASE (Required)
# =====================================
# PostgreSQL connection string
# For Render PostgreSQL (production):
DATABASE_URL="postgresql://user:password@dpg-xxxxx.render.com:5432/dbname?sslmode=require"
#
# For local PostgreSQL (development):
# DATABASE_URL="postgresql://postgres:password@localhost:5432/cityflow_invoices"

# =====================================
# GOOGLE GEMINI API (Required)
# =====================================
# Get your free API key at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY="your_gemini_api_key_here"

# =====================================
# JWT SECRETS (Required)
# =====================================
# Generate random secrets with: openssl rand -base64 32
# Use different secrets for access and refresh tokens
JWT_ACCESS_SECRET="your_long_random_access_secret_min_32_chars"
JWT_REFRESH_SECRET="your_long_random_refresh_secret_min_32_chars"

# =====================================
# BACKEND CONFIGURATION (Optional)
# =====================================
PORT=3000
UPLOAD_DIR=./uploads
FRONTEND_URL="http://localhost:5173"
NODE_ENV="development"

# =====================================
# FRONTEND (Optional - usually not needed with proxy)
# =====================================
# Only needed if deploying frontend separately from backend
# VITE_API_URL="http://localhost:3000"

# =====================================
# DEPLOYMENT NOTES
# =====================================
# Backend (Render):
#   - Set DATABASE_URL to your Render PostgreSQL External URL
#   - Set NODE_ENV=production
#   - Add all JWT secrets and GEMINI_API_KEY
#
# Frontend (Render):
#   - Set VITE_API_URL to backend URL (e.g., https://cityflo-assignment-backend.onrender.com)
#   - Otherwise, frontend proxies to localhost:3000 in dev
#
# Security:
#   - Keep secrets long and random (32+ characters)
#   - Rotate secrets regularly in production
#   - Use different values for dev/staging/production
